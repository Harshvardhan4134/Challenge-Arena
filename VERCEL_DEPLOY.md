# Vercel Deployment (Frontend)

This repo is configured to deploy the `challenge-arena` frontend from the monorepo root.

## 1) Push to your new GitHub repo

Push this codebase to GitHub first.

## 2) Create project in Vercel

1. Go to Vercel dashboard -> **Add New...** -> **Project**.
2. Import your GitHub repo.
3. Keep root directory as repository root (`/`).
4. Vercel will use `vercel.json` in this repo:
   - install command: `pnpm install`
   - build command: `pnpm --filter @workspace/challenge-arena run build`
   - output directory: `artifacts/challenge-arena/dist/public`

## 3) Add required environment variables in Vercel

Set these in Project Settings -> Environment Variables:

- `VITE_API_BASE_URL` = your deployed backend URL (example: `https://your-api.onrender.com`)
- `VITE_FIREBASE_API_KEY`
- `VITE_FIREBASE_AUTH_DOMAIN`
- `VITE_FIREBASE_PROJECT_ID`
- `VITE_FIREBASE_STORAGE_BUCKET`
- `VITE_FIREBASE_MESSAGING_SENDER_ID`
- `VITE_FIREBASE_APP_ID`
- `VITE_FIREBASE_MEASUREMENT_ID`

Then redeploy.

## 4) Backend URL note

Vercel deploys frontend here. Your Express backend should be deployed separately (Render/Railway/Fly/etc.).

- Frontend URL: generated by Vercel (example: `https://your-app.vercel.app`)
- Backend URL: generated by your backend host (example: `https://your-api.onrender.com`)

The frontend calls backend using `VITE_API_BASE_URL`.
